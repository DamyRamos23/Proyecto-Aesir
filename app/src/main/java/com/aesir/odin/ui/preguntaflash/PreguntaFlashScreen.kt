package com.aesir.odin.ui.preguntaflash

import androidx.navigation.NavHostController
import androidx.compose.runtime.Composable
import com.aesir.odin.ui.components.PantallaProximamente

/**
 * Destino de rutaPreguntaFlash. Fuera del alcance de CU-1; se deja como
 * placeholder para que el grafo de navegación quede completo.
 */
@Composable
fun PreguntaFlashScreen(navController: NavHostController) {
    PantallaProximamente(navController, "Pregunta flash")
}
